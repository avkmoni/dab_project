# This is a Databricks asset bundle definition for dab_project.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: dab_project
  uuid: 102434f0-cc58-48f8-9cda-fff4d02780d6

artifacts:
  python_artifact:
    type: whl
    build: uv build --wheel

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-4202402577907414.14.azuredatabricks.net

  prod:
    mode: production
    workspace:
      host: https://adb-4202402577907414.14.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/vaikunda.moni@dxc.com to make sure we only have a single copy.
      root_path: /Workspace/Users/vaikunda.moni@dxc.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: vaikunda.moni@dxc.com
        level: CAN_MANAGE
